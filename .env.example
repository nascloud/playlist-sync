# .env.example
# 请将此文件复制为 .env 并填入您的配置信息

# =========================================
# 安全设置 (必需)
# =========================================

# 用于保护您的应用程序和加密敏感信息的密钥。
# 您必须设置此项。
# 建议使用一个长而随机的字符串，例如，您可以通过 `openssl rand -hex 32` 生成一个。
SECRET_KEY=

# 用于访问应用界面的密码。
# 您必须设置此项。
APP_PASSWORD=

# =========================================
# Plex 服务器设置 (可选)
# =========================================
# 只有在希望应用启动时自动连接到Plex时才需要填写这些。
# 您也可以稍后在应用的用户界面中进行配置。

# 您的Plex服务器的完整URL地址。
# 例如: http://192.168.1.100:32400
PLEX_URL=

# 用于访问Plex服务器的API令牌。
# 您可以参考Plex的官方文档来获取此令牌。
PLEX_TOKEN=

# =========================================
# 下载器设置 (可选)
# =========================================
# 这些设置用于配置歌曲下载功能。
# 您也可以稍后在应用的用户界面中进行配置。

# 第三方下载服务的API密钥。
DOWNLOADER_API_KEY=

# 下载的音乐文件将要保存到的路径。
# 请确保此路径对于运行应用的容器或进程是可访问和可写的。
# 例如: /downloads
DOWNLOADER_PATH=

# =========================================
# 高级设置 (可选)
# =========================================
# 大多数情况下，您不需要更改这些设置。

# JWT访问令牌的过期时间（分钟）。
# 默认值: 10080 (即7天)
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# 用于JWT签名的加密算法。
# 默认值: HS256
ALGORITHM=HS256
